events {}

http {
    server {
        listen 8080;

        location / {
            proxy_pass http://127.0.0.1:8000;  # Docker: change to http://server:8000
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
